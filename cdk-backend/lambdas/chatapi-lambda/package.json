{
  "name": "chatapi-lambda",
  "version": "1.0.0",
  "description": "Chat API Lambda",
  "main": "index.js",
  "author": "mcosic",
  "repository": "https://github.com/amazon-connect/video-call-escalation",
  "license": "MIT-0",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.8.50",
    "eslint": "^7.19.0"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --external:aws-sdk --external:/opt/* --outdir=build --target=node12"
  }
}
